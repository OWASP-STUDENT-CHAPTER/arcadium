class Tile {
    constructor(name, points, x, y) {
        this.name = name;
        this.points = points;
        this.pos = { x, y };
    }

    // render =()=>{

    // }
}

const a = new Tile("name", 100, 100, 100);
console.log(a);
